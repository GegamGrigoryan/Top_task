image: Visual Studio 2015 # образ для сборки

stack: node 19.7.0  # окружение

branches:
  only:
    - main  # ветка git

cache: node_modules  # кеширование

install:
  - yarn install  # команда установки зависимостей

build: off  # отключаем встроенную в appveyor систему сборки

build_script:
  - yarn run build   # команда сборки

# test_script:
#   - npm run lint && npm test  # скрипт тестирования

deploy_script:  # скрипт развёртывания
  - git config --global user.name GegamGrigoryan
  - git config --global user.email m-mik@ro.ru
  - git checkout -b deploy
  - git add .
  - git commit -m "Deploy to GitHub Pages"
  - git push --force --quiet "https://${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}@github.com" > "https://github.com/GegamGrigoryan/Top_task"
  
