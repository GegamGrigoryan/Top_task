image: Ubuntu1804 # образ для сборки

stack: node 14.17.0 # окружение

branches:
  only:
    - main # ветка git

cache: node_modules # кеширование

build: off # отключаем встроенную в appveyor систему сборки

build_script:
  - yarn install
  - yarn run build # команда сборки

test_script:
test:
  off
  #  - yarn run test && npm test # скрипт тестирования

deploy_script: # скрипт развёртывания
  - git config --global user.email "mika_g@internet.ru"
  - git config --global user.name "GegamGrigoryan"
  - git checkout -b deploy
  - git add -A
  # - git commit --allow-empty -m "."
  - git -c http.extraheader="AUTHORIZATION: bearer YOUR_GITHUB_TOKEN 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"
  - git push -f origin deploy
  
