image: Ubuntu1804  # образ для сборки

stack: node 19.9.0  # окружение

branches:
  only:
    - main  # ветка git

cache: node_modules  # кеширование

install:
  - npm install  # команда установки зависимостей

build: off  # отключаем встроенную в appveyor систему сборки

build_script:
  - npm run build   # команда сборки
  - nvm use --delete-prefix v19.9.0

# test_script:
#   - npm run lint && npm test  # скрипт тестирования

deploy_script:  # скрипт развёртывания
  - git config --global user.name "GegamGrigoryan"
  - git config --global user.email "m-mik@ro.ru"
  - git checkout -b deploy
  - git add .
  - git commit -m "Deploy to GitHub Pages"
  - git push --force --quiet "https://${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}@github.com/GegamGrigoyan/https://github.com/GegamGrigoryan/Top_task"
  
